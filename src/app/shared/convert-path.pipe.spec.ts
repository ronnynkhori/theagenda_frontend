import { ConvertPathPipe } from './convert-path.pipe';

describe('ConvertPathPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertPathPipe();
    expect(pipe).toBeTruthy();
  });
});
